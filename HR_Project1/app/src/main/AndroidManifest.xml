<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hr_project1">
    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DeviceTakeInOutActivity"
            android:label="@string/app_name"
            android:parentActivityName=".LoginMainActivity"
            android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".Settings_Activity"
            android:label="@string/settings"
            android:parentActivityName=".LoginMainActivity"
            android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".Profile_Activity"
            android:label="@string/profile"
            android:parentActivityName=".LoginMainActivity"
            android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".RegistrationForm_Activity"
            android:label="HR_Project1"
            android:parentActivityName=".LoginMainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".LoginMainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>